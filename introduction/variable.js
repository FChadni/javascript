"use strict";

/** Intro to variables
 * Variables stores information which can be accessed and changed later.
 */

/** Rules for naming variable ~ camelCase or snake_case
 * no-numbers at beginning, no-symbol except $ and _, no-space
 */

// Declare a variable
var firstName = "Jan";
// Access variable
console.log(firstName);
// Change variable
firstName = "Janny";
console.log(firstName);

/** Let keyword ~ let used more over var
 * cannot re-declare using let, however value can be changed.
 */

/** const keyword ~ cannot modify
 *
 */